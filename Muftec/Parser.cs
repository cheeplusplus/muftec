using System;
using System.Collections.Generic;
using MuftecLib;
using MuftecBCL;

namespace Muftec
{
	class Parser
	{
		/// <summary>
		/// Parse a string into a runtime stack
		/// </summary>
		/// <param name="input">Input stack text</param>
		/// <returns>Output stack</returns>
		static Stack<MuftecStackItem> ParseString(string input)
		{
			var execStack = new Stack<MuftecStackItem>();

			return execStack;
		}
	}
}
